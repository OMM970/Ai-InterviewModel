spring:

  application:
    name: Ai-Interview

  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/interview_schedule

  jpa:
    hibernate:
      ddl-auto: update

  data:

    mongodb:
      host: localhost
      port: 27017
      database: credential_db

    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: csflight534@gmail.com
    password: wrguultwxqjxqipl

    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 5000
      mail.smtp.writetimeout: 5000

server:
  port: 2999

aws:

  access:
    key:

  secret:
    key:

  region: eu-north-1

  s3:
    bucket:
      name: amzn-s3-projectai